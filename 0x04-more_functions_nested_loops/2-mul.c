#include "main.h"

/**
 * mul - multipies 2 integers
 * @a: first number to multiply
 * @b: second number to multiply
 * Return: The result of mulptiplication between a param and b param
 */

int mul(int a, int b)

{
	return (a * b);
}

#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10 to 0.
 *
 * Return: Always 0 (Sucess)
 */
int main(void)
{
	int n;

	for (n = 48; n < 58; n++)

	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}

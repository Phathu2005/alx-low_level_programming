#include "main.h"

/**
 * _abs - Function that computes the absolute value of an integer
 * @n: the int to check
 * Return:the absolute value if int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
